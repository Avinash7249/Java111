package com.masai;

public class Question1 {
	
	// What is Tight coupling and loose coupling?
	
	//Tight coupling:suppose, if any changes are made in the dependency class
	//and if it is forced to do the changes inside the dependent class  then
	//we can say that both classes are tightly coupled with each other.
	
	//loose coupling:suppose, if any changes are made in the dependency class
	//and if it doesn't force to do the changes inside the dependent class  then
	//we can say that both classes are loosely coupled with each other.


}
