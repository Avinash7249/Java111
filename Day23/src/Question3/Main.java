package Question3;

public class Main {
	//Explain Transient KeyWord.
	
		//This keyword is applicable only for the variables. at the time of serialization
		//if we want hide any data(perticular) of custmour then that varible put as transient mode
		//then nobudy see that data
		//if we apply transient keyword to static veriable then static variables are 
		//not part of object, hence they will also not participate in serialization.

		
}
